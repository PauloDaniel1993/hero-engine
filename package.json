{
  "name": "hero-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Foundry VTT plugin engine for bespoke epic character/weapon mechanics (dnd5e)",
  "scripts": {
    "build": "vite build && npm run types && node scripts/check-plugin-imports.mjs",
    "types": "tsc -p tsconfig.types.json",
    "check": "tsc -p tsconfig.json --noEmit && node scripts/check-plugin-imports.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
